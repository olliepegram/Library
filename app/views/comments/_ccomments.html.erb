<h2>Add a comment:</h2>
<%= form_for([@book, @book.comments.build]) do |f| %>
<div class="form-group">
    <p>
        <%= f.label :body %><br>
        <%= f.text_area :body, class: "form-control" %>
    </p>
    <p>
        <%= f.submit %>
    </p>
</div>
<% end %>
